Place game images here (optional — the game uses public URLs by default).

To use local images:
1. Add .jpg/.png files here
2. Update the IMAGE_POOL in app.js to reference them:
   { url: "assets/images/keyboard.jpg", answer: "keyboard" }
